Installation
============

Install ``aliaser`` from PyPI with ``pip``:

.. code-block:: bash

   pip install aliaser

Alternatively, add it to a Poetry project:

.. code-block:: bash

   poetry add aliaser

